<h2 class="center">我要约饭</h2>
<div class="container">
    <div class="row">
        <div class="col-md-8 col-md-offset-2">
            <%= simple_form_for [:account, @post] do |f| %>

            <div class="group">
                <%= f.input :title, label: '主题' %>
            </div>

            <div class="group">
                <%= f.input :description, label: '详细信息' %>
            </div>

            <div class="group">
              <p>
               <strong>约饭地点</strong>
              </p>
             <%= f.radio_button :eat_venue, '101咖啡',checked: true %><strong>101咖啡</strong><br>
             <%= f.radio_button :eat_venue, '汤城小厨' %><strong>汤城小厨</strong><br>
             <%= f.radio_button :eat_venue, '星巴克' %><strong>星巴克</strong>
           </div>

           <div class="group">
             <p>
              <strong>约饭日期</strong>
             </p>

             <%= radio_button(:post, :eat_day ,Time.now.end_of_week()-1.days)%><strong>本周六<%= (Time.now.end_of_week()-1.days).strftime("%Y/%m/%d") %></strong><br>
             <%= radio_button(:post, :eat_day ,Time.now.end_of_week()) %><strong>本周日<%= Time.now.end_of_week().strftime("%Y/%m/%d") %></strong>


           </div>

           <div class="group">
               <%= f.input :image, as: :file, label: '图片' %>
           </div>
           <%= f.submit "提交", class: "btn btn-default", data: { disable_with: "Submitting..." } %>

            <% end %>
        </div>
    </div>
</div>
