<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>我的约饭记录</title>
  </head>

   <body>
      <div class="container">
        <div class="row">
         <div class="bread-user">
            <div class="brend-word">
              <span class="bread-one">
                  <a href="/">首页</a>
              </span>
              <span class="bread-two">»</span>
              <span class="bread-three">个人中心</span>
           </div>
         </div>

          <div id="content">
               <% @posts.each do |post| %>
                 <div class="col-md-4">
                    <div class="img-account">
                      <%= link_to account_post_path(post) do %>
                        <% if post.image.present? %>
                            <%= image_tag(post.image.thumb.url) %>
                        <% else %>
                            <%= image_tag("logo-nopic3.png", class: "thumbnail") %>
                        <% end %>
                        <div class="desc">
                           <h3 class="titleh3"> <%= post.title %> </h3>
                           <h4 class="titleh4"> <strong> <%= post.eat_venue %> </strong></h4>
                           <h4 class="titleh4"><strong> <%= post.eat_day %> </strong></h4>
                           <p>    <%=link_to("删除记录", account_post_path(post),
                                 method: :delete, data: { confirm: "Are you sure?" })%>
                           </p>
                        </div>
                          <% end %>
                      </div>
                   </div>
                <% end %>
             </div>
           </div>
        </div>

  </body>
</html>
