<div class="container">
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
            <%= simple_form_for @interest do |f| %>
            <h4 class="center">兴趣爱好</h4>
            <%= f.input :name, label: "." %>
            <%= f.submit "提交", class: "btn btn-default center-block", data: { disable_with: "Submitting..." } %>
            <% end %>
        </div>

    </div>
</div>
