<nav class="navbar navbar-default" role="navigation">
    <div class="container">

         <div class="navbar-header">
            <a class="navbar-brand projname" href="/">Eatgether</a>

         </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                 <li> <%=  link_to("最新约饭",posts_path)%></li>
              <% if !current_user %>
                  <li><%= link_to("注册", new_user_registration_path) %> </li>
                  <li><%= link_to("登入", new_user_session_path) %></li>
                <% else %>
                  <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        Hi!, <%= current_user.nameNick %>
                        <b class="caret"></b>
                    </a>
                   <ul class="dropdown-menu">
                       <li> <%=  link_to("我的预约",account_posts_path)%></li>
                       <li> <%=  link_to("我的约饭",account_orders_path)%></li>
                       <li> <%=  link_to("个人中心",account_users_path)%></li>
											 <li class="divider"></li>
											 <% if current_user.admin? %>
													<li> <%= link_to("管理后台", admin_users_path) %> </li>
													<li class="divider"></li>
												<% end %>
                       <li> <%= link_to("登出", destroy_user_session_path, method: :delete) %> </li>
                    </ul>
                  </li>
                <% end %>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
