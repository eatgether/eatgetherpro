<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">

         <div class="navbar-header">
            <a class="navbar-brand projname" href="/"><img src="assets/logo1-white.png" class="logonav">Eatgether</a>
         </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav navbar-right">
                 <li> <%= link_to("<i class='fa fa-cutlery'></i>  最新约饭".html_safe,posts_path)%></li>
              <% if !current_user %>
									<!-- <%#= link_to "#signupModal", 'data-toggle' => 'modal' do %>
									<li><i class="fa fa-user"> </i>注册</li>
									<%# end %> -->
                  <li><%= link_to("<i class='fa fa-user'></i>  注册".html_safe, "#signupModal", 'data-toggle' => "modal") %></li>
									<li><%= link_to("<i class='fa fa-sign-in'></i>  登陆".html_safe, "#loginModal", 'data-toggle' => "modal") %></li>
                <% else %>
                  <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        Hi, <%= current_user.email %>
                        <b class="caret"></b>
                    </a>
                   <ul class="dropdown-menu">
                       <li> <%=  link_to("我的预约",account_posts_path)%></li>
                       <li> <%=  link_to("我的约饭",account_order_twos_path)%></li>
                       <li> <%=  link_to("个人中心",account_users_path)%></li>
											 <li class="divider"></li>
											 <% if current_user.admin? %>
													<li> <%= link_to("管理后台", admin_users_path) %> </li>
													<li class="divider"></li>
												<% end %>
                       <li> <%= link_to("登出", destroy_user_session_path, method: :delete) %> </li>
                    </ul>
                  </li>
                <% end %>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>

<%= render "common/signup" %>
<%= render "common/login" %>
